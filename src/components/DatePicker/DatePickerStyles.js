import {
    StyleSheet
} from "react-native";

const styles = StyleSheet.create({
    btnData: {
        width: 330,
        height: 50,
        backgroundColor: 'white',
        borderRadius: 10,
        justifyContent: 'center',
        marginTop: 25,
        shadowOffset: {
            width: 6,
            height: 6,
        },
        shadowOpacity: 0.20,
        borderColor: '#C6C6C6',
        marginBottom: 15,
        borderWidth: 1,
    },
})

export default styles;